## v1.5.0 — Phase 18: every indicator becomes a signal method, success % beside every signal, global clock strip
- **31 new indicator strategies** (`strategies/indicator_signals.py`, category "Indicator"): WMA/ZLEMA/VWMA/T3/McGinley/SuperSmoother, LinReg-channel fade, HalfTrend flip, Vortex, TRIX, Coppock, Schaff, Ultimate, AO saucer, Accelerator, Fisher, QQE, Elder-Ray, Mass-Index bulge, Z-score reversion, HV squeeze, A/D divergence, VPT, NVI, Klinger, RVOL breakout, Heikin-Ashi run, ZigZag structure, Volume-Profile value area, Fib golden pocket, RS leader. Every catalog indicator now has a documented signal rule → **163 strategies**. 14 of them join the Fast-Signals vote pool.
- **Success rate next to every signal** (`core/success.py`): WR · PF · n measured by the real fee-aware backtester on the exact symbol/timeframe, filled automatically by the chart page, scanner and live radar, and in bulk by *Settings ▸ compute success rates* (all strategies × 5 majors × 4 TFs, background). Shown in the strategy picker `[32%]`, chart summary, scanner/live tables, Fast-Signals "agreeing methods" column, and a new Academy success panel per strategy (per-market table + share of profitable markets). Playbook OOS interval appended where it exists.
- **Clock strip** at the top of the main window: computer clock (master clock of every section, with tz name) + world clock UTC/NY/LDN/FRA/THR/DXB/TYO/SYD with session-open marks. New *Settings ▸ Time display* (local/UTC) applies to all chart axes and tables; backtest trade list now in local time.
- **Fix**: `indicators2.zigzag` never emitted pivots (trend state bug) — now a correct % reversal ZigZag; pivot high/low classification fixed. Fib golden-pocket adds freshness + EMA100 gate.
- **Honesty**: standalone indicator rules lose after fees on BTC 1h (PF 0.5–0.9, WR 25–46 %, best: mass_index_bulge PF 0.90, ultimate_osc 0.87); they are meant as confluence votes and are reported with their measured numbers, never hidden.

