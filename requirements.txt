PyQt6>=6.5
pyqtgraph>=0.13
pandas>=2.0
numpy>=1.24
yfinance>=0.2.40
requests>=2.28
pyarrow>=12
beautifulsoup4>=4.12
lxml>=4.9
scikit-learn>=1.3
websocket-client>=1.6
# --- Advanced features (anti-filter, Iran Gold, persistence) ---
# No extra dependencies required - uses standard library + existing packages
# Optional for enhanced proxy detection on Windows:
# winshell; sys_platform == "win32"
# pywin32; sys_platform == "win32"
